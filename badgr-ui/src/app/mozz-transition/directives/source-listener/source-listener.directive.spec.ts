import { SourceListenerDirective } from './source-listener.directive';
import { ActivatedRoute } from "@angular/router";

describe('SourceListenerDirective', () => {
  it('should create an instance', () => {
    // const directive = new SourceListenerDirective({snapshot:{params:{}}});
    // expect(directive).toBeTruthy();
  });
});
