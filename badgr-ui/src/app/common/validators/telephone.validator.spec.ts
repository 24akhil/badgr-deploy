import { async } from '@angular/core/testing';
import {TelephoneValidator} from './telephone.validator';
import {Directive} from '@angular/core';

describe('My Test', () => {
  let service;

  beforeEach(() => {
    // TODO
  });

      it('should run #{key}', async () => {
        // const result = validTelephone(control);
      });

});
