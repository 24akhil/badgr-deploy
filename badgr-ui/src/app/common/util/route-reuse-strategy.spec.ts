import { async } from '@angular/core/testing';
import {BadgrRouteReuseStrategy} from './route-reuse-strategy';
import {Directive} from '@angular/core';

xdescribe('My Test', () => {
  let service;

  beforeEach(() => {
    // TODO
  });

      it('should run #{key}', async () => {
        // const result = shouldReuseRoute(future, curr);
      });

});
